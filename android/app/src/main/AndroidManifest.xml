<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- RPC calls (solana client) and the MWA local-socket handshake need
         network access; explicit here since release builds don't inherit
         the debug-only INTERNET permission. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <!-- flutter_contacts: picking members when creating/inviting to a circle.
         Not auto-merged by the plugin, so declared explicitly. -->
    <uses-permission android:name="android.permission.READ_CONTACTS"/>

    <!-- local_auth: biometric confirmation before signing/sending a
         contribution transaction (declared explicitly per plugin README;
         local_auth_android also merges this in). -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC"/>

    <!-- flutter_local_notifications: round/payout reminders. Runtime prompt
         required on Android 13+ (API 33+); permission itself is also merged
         in by the plugin. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <!-- Mobile Wallet Adapter: the com.solanamobile:mobile-wallet-adapter-clientlib
         AAR (pulled in by solana_mobile_client) already merges the required
         <queries> entry for android.intent.action.VIEW + solana-wallet://
         scheme so the app can detect/launch installed MWA wallet apps (e.g.
         the Seeker/Saga default wallet) under Android 11+ package-visibility
         rules. No manual queries block needed here. -->

    <application
        android:label="ajose"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
